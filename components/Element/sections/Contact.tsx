import { SectionLayouts, Title } from '../Layout';

export const Contact = () => {
  return (
    <SectionLayouts id="contact" className="">
      <Title>CONTACT</Title>
    </SectionLayouts>
  );
};
